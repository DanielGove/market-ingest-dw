#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ROOT_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"
PY="${PY:-$ROOT_DIR/venv/bin/python}"

if [ ! -x "$PY" ]; then
  PY="python3"
fi

exec "$PY" "$SCRIPT_DIR/backtest_segments.py" "$@"

