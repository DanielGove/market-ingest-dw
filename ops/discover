#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ROOT_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"
PY="${PY:-$ROOT_DIR/venv/bin/python}"

if [ ! -x "$PY" ]; then
  PY="python3"
fi

PYTHONPATH="$ROOT_DIR" exec "$PY" "$ROOT_DIR/runtime/discover.py" "$@"
